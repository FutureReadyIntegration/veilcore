actions:
- type: authenticate
- type: render
- type: execute
affirmation: Through me, operators see The Veil.
dependencies:
- auth
- rbac
- gateway
description: Secure web portal for Veil OS administration.
glyph: "\U0001F300"
inputs:
- format: json
  source: /opt/veil_os/sockets/portal.sock
name: portal
organ: portal
outputs:
- format: jsonl
  path: /opt/veil_os/ledger/portal.log
symbolic_role: window
technical_role: web_portal
tier: interface
triggers:
- type: user_session
- type: action_request
version: 1.0
